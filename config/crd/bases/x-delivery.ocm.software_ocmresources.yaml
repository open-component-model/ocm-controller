---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: ocmresources.x-delivery.ocm.software
spec:
  group: x-delivery.ocm.software
  names:
    kind: OCMResource
    listKind: OCMResourceList
    plural: ocmresources
    singular: ocmresource
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OCMResource is the Schema for the ocmresources API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: OCMResourceSpec defines the desired state of OCMResource
            properties:
              resource:
                description: Resource names a Source that this OCMResource watches.
                type: string
            required:
            - resource
            type: object
          status:
            description: OCMResourceStatus defines the observed state of OCMResource
            properties:
              ready:
                description: Ready denotes the state of processing a Source.
                type: boolean
              snapshot:
                description: Snapshot is a snapshot of a Source in the in-cluster
                  OCI storage.
                type: string
            required:
            - ready
            - snapshot
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
