---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: ocmcomponentversions.delivery.ocm.software
spec:
  group: delivery.ocm.software
  names:
    kind: OCMComponentVersion
    listKind: OCMComponentVersionList
    plural: ocmcomponentversions
    singular: ocmcomponentversion
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OCMComponentVersion is the Schema for the OCMComponentVersions
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: OCMComponentVersionSpec defines the desired state of OCMComponentVersion
            properties:
              interval:
                description: A Duration represents the elapsed time between two instants
                  as an int64 nanosecond count. The representation limits the largest
                  representable duration to approximately 290 years.
                format: int64
                type: integer
              name:
                type: string
              repository:
                description: Repository defines the OCM Repository.
                properties:
                  secretRef:
                    description: SecretRef is a reference to a secret used to access
                      the OCI repository.
                    properties:
                      name:
                        type: string
                    required:
                    - name
                    type: object
                  url:
                    type: string
                required:
                - secretRef
                - url
                type: object
              verify:
                description: Verify holds the secret which contains the signing and
                  verification keys.
                properties:
                  secretRef:
                    description: SecretRef is a reference to a secret used to access
                      the OCI repository.
                    properties:
                      name:
                        type: string
                    required:
                    - name
                    type: object
                required:
                - secretRef
                type: object
              version:
                type: string
            required:
            - interval
            - name
            - repository
            - verify
            - version
            type: object
          status:
            description: OCMComponentVersionStatus defines the observed state of OCMComponentVersion
            properties:
              componentDescriptor:
                type: string
              deployPackage:
                description: 'TODO: DeployPackage could be a configMap....'
                type: string
              verified:
                type: boolean
            required:
            - componentDescriptor
            - deployPackage
            - verified
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
