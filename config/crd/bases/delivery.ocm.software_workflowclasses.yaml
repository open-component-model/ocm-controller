---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: workflowclasses.delivery.ocm.software
spec:
  group: delivery.ocm.software
  names:
    kind: WorkflowClass
    listKind: WorkflowClassList
    plural: workflowclasses
    singular: workflowclass
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WorkflowClass is the Schema for the actions API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: WorkflowClassSpec defines the desired state of WorkflowClass
            properties:
              stages:
                type: object
              workflow:
                items:
                  properties:
                    input:
                      type: string
                    name:
                      type: string
                  required:
                  - name
                  type: object
                type: array
            required:
            - stages
            - workflow
            type: object
          status:
            description: WorkflowClassStatus defines the observed state of WorkflowClass
            properties:
              ready:
                type: boolean
              snapshot:
                type: string
            required:
            - ready
            - snapshot
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
