---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: installationworkflows.delivery.ocm.software
spec:
  group: delivery.ocm.software
  names:
    kind: InstallationWorkflow
    listKind: InstallationWorkflowList
    plural: installationworkflows
    singular: installationworkflow
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: InstallationWorkflow is the Schema for the actions API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: InstallationWorkflowSpec defines the desired state of InstallationWorkflow
            properties:
              classResource:
                description: ClassResource defines the class resource that has been
                  generated for the given component.
                properties:
                  name:
                    type: string
                  type:
                    type: string
                required:
                - name
                - type
                type: object
              component:
                type: string
              overrides:
                description: Overrides define user given values to be applied to package
                  defined values in the Class.
                properties:
                  actions:
                    x-kubernetes-preserve-unknown-fields: true
                required:
                - actions
                type: object
              serviceAccount:
                type: string
              version:
                type: string
            required:
            - classResource
            - component
            - overrides
            - serviceAccount
            - version
            type: object
          status:
            description: InstallationWorkflowStatus defines the observed state of
              InstallationWorkflow
            properties:
              ready:
                type: boolean
              snapshot:
                type: string
            required:
            - ready
            - snapshot
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
