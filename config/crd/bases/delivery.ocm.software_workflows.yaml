---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: workflows.delivery.ocm.software
spec:
  group: delivery.ocm.software
  names:
    kind: Workflow
    listKind: WorkflowList
    plural: workflows
    singular: workflow
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Workflow is the Schema for the actions API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: WorkflowSpec defines the desired state of Workflow
            properties:
              classResource:
                description: ClassResource defines the class resource that has been
                  generated for the given component.
                properties:
                  name:
                    type: string
                  type:
                    type: string
                required:
                - name
                - type
                type: object
              componentRef:
                description: ComponentRef defines a reference to a component.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                - namespace
                type: object
              overrides:
                description: Overrides define user given values to be applied to package
                  defined values in the Class.
                properties:
                  actions:
                    x-kubernetes-preserve-unknown-fields: true
                required:
                - actions
                type: object
              serviceAccount:
                type: string
            required:
            - classResource
            - componentRef
            - overrides
            - serviceAccount
            type: object
          status:
            description: WorkflowStatus defines the observed state of Workflow
            properties:
              ready:
                type: boolean
              snapshot:
                type: string
            required:
            - ready
            - snapshot
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
