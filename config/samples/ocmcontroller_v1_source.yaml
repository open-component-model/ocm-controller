apiVersion: x-delivery.ocm.software/v1alpha1
kind: Source
metadata:
  name: ui-server
  namespace: wego-system
spec:
  componentRef:
    name: weave-gitops
    namespace: wego-system
  providerRef:
    apiVersion: x-delivery.ocm.software/v1alpha1
    kind: OCMResource
    name: wge-ui-server
status:
  ready: false
  snapshot: string
