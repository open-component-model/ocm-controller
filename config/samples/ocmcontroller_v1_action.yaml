apiVersion: x-delivery.ocm.software/v1alpha1
kind: Action
metadata:
  name: localize
  namespace: wego-system
spec:
  componentRef:
    name: wge
    namespace: wego-system
  sourceRef:
    name: wge-admin-source
  providerRef:
    apiVersion: x-delivery.ocm.software/v1alpha1
    kind: Localization
    name: wge-localizer
status:
  ready: false
  snapshot: string
