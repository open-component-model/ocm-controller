apiVersion: delivery.ocm.software/v1alpha1
kind: Configuration
metadata:
  name: configuration-unsigned
  namespace: ocm-system
spec:
  interval: 1m
  sourceRef:
    kind: Localization
    name: localization-unsigned
  configRef:
    kind: ComponentVersion
    name: podinfo-unsigned
    resourceRef:
      name: config-backend
      version: latest
  values:
    message: "This is a test message Unsigned"
