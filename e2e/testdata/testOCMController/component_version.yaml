apiVersion: delivery.ocm.software/v1alpha1
kind: ComponentVersion
metadata:
  name: podinfo-unsigned
  namespace: ocm-system
spec:
  interval: 5s
  references:
    expand: true
  component: "component.name.unsigned/podinfo"
  version:
    semver: ">=v1.0.0"
  repository:
    url: "http://registry.ocm-system.svc.cluster.local:5000"
