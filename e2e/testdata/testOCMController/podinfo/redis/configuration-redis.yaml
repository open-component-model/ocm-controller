apiVersion: delivery.ocm.software/v1alpha1
kind: Configuration
metadata:
  name: configuration-redis
  namespace: ocm-system
spec:
  interval: 1m
  sourceRef:
    kind: Localization
    name: localization-redis
  configRef:
    kind: ComponentVersion
    name: podinfo
    resourceRef:
      name: config-redis
      version: latest
      referencePath:
        - name: redis
  values:
    message: "This is a test message redis"
