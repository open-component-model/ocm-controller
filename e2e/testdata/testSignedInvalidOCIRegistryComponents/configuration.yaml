apiVersion: delivery.ocm.software/v1alpha1
kind: Configuration
metadata:
  name: deployment-signed-invalid
  namespace: ocm-system
spec:
  interval: 1m
  sourceRef:
    kind: Localization
    name: deployment-signed-invalid
  configRef:
    kind: ComponentVersion
    name: podinfo-signed-invalid
    resourceRef:
      name: config
      version: latest
  values:
    message: "This is a test message SignedInvalid"
