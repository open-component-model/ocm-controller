apiVersion: delivery.ocm.software/v1alpha1
kind: Configuration
metadata:
  name: deployment-signed
  namespace: ocm-system
spec:
  interval: 1m
  sourceRef:
    kind: Localization
    name: deployment-signed
  configRef:
    kind: ComponentVersion
    name: podinfo-signed
    resourceRef:
      name: config
      version: latest
  values:
    message: "This is a test message Signed"
